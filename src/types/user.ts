export type User = {
  id: number;
  name: string;
  email: string;
  avatar: string;
  company: string;
  role: string;
}

export type UserAuthData = {
  id: number;
  firstName: string;
  secondName: string;
};
